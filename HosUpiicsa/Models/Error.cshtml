@model HosUpiicsa.Models.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error</h1>
<h2 class="text-danger">Ocurrió un error al procesar tu solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo Desarrollo</h3>
<p>
    Cambiando a <strong>Desarrollo</strong> se mostrará más información detallada sobre el error.
</p>
<p>
    <strong>El entorno de desarrollo no debe estar activado en aplicaciones publicadas.</strong>
    Puede resultar en que se muestre información confidencial de los usuarios finales.
</p>